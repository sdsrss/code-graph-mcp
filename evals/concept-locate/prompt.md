---
description: "Find a function by what it does, without its name."
max_turns: 40
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Bash, Agent]
tags: [structural]
---

This repository is a Rust project under src/. Where is the function that estimates how many tokens a JSON value will cost, which is used to decide whether a tool result gets compressed? Give the function name and its path relative to the repository root.
