---
description: "Control — a plain lookup grep answers in one call. Expect Δ ≈ 0; compare turns and cost to see the plugin's overhead."
max_turns: 40
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Bash, Agent]
tags: [control]
---

This repository is a Rust project under src/. What is the value of the constant `CHARS_PER_TOKEN`, and in which file is it defined? Give the path relative to the repository root.
