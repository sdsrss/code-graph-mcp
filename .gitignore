# Build artifacts
/target

# Runtime data
.code-graph/
models/

# Temp / tools
.tmp/
.gsd/

# Hidden files and directories (IDE, OS, tool configs)
.*
!.gitignore
!.npmignore
!.github/

# Documentation (local only)
docs/

# AI assistant config (CLAUDE.md is tracked — project instructions for code-graph)

# npm binary (built via scripts/copy-binary.js, not committed)
bin/code-graph-mcp
bin/code-graph-mcp.exe

# Platform-specific npm binaries (built during CI)
npm/*/code-graph-mcp
npm/*/code-graph-mcp.exe

# npm artifacts
node_modules/
*.tgz
